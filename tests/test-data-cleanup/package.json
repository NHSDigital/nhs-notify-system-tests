{
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.806.0"
  },
  "description": "Cleans up data left over from running the product tests",
  "devDependencies": {
    "@aws-sdk/types": "^3.775.0",
    "@tsconfig/node20": "^20.1.5",
    "@types/node": "^20.13.1",
    "tsx": "^4.19.4",
    "typescript": "^5.8.2"
  },
  "main": "src/cleanup.ts",
  "name": "test-data-cleanup",
  "scripts": {
    "lint": "eslint ./src",
    "lint:fix": "eslint ./src --fix",
    "cleanup": "tsx src/cleanup.ts",
    "typecheck": "tsc --noEmit"
  }
}
